/**
 * Global application constants for the MariaDB AI Demo.
 */

// Toggle this to false to test with real backend APIs
export const IS_DEMO_MODE = true;

// Key for deployment status in local storage
export const DEMO_DEPLOYED_KEY = 'maria_db_demo_deployed_v2';
